// Write code below 💖

const pig = {
  name: "bob",
  type: "pig",
  age: 5,
  makeSound () {
    console.log(`${pig.name} is a ${pig.age} year old ${pig.type} that goes oinc!`)
  }
}
pig.makeSound()

const sheep = {
  name: "xipi",
  type: "sheep",
  age: 8,
  makeSound () {
    console.log(`${sheep.name} is a ${sheep.age} year old ${sheep.type} that goes uar`)
  }
}
sheep.makeSound()

const dog = {
  name: "chorrin",
  type: "dog",
  age: 8,
  makeSound () {
  console.log(`${dog.name} is a ${dog.age} year old ${dog.type} that goes woof!`)
  }
}
dog.makeSound()